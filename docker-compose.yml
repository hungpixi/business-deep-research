services:
  # === Main App ===
  app:
    build: .
    container_name: deep-research
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./output:/app/output # Persist reports
      - ./knowledge:/app/knowledge # Persist knowledge edits
      - ./cache:/app/cache # Persist search cache
    environment:
      - PYTHONUNBUFFERED=1
    # Memory limit for 1GB RAM VPS
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M
    # Connect to Antigravity on host machine
    extra_hosts:
      - "host.docker.internal:host-gateway"
  # === Optional: Nginx reverse proxy with SSL ===
  # Uncomment if deploying with custom domain
  # nginx:
  #   image: nginx:alpine
  #   container_name: deep-research-nginx
  #   restart: unless-stopped
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/conf.d/default.conf
  #     - ./certbot/conf:/etc/letsencrypt
  #     - ./certbot/www:/var/www/certbot
  #   depends_on:
  #     - app
